#include<stdio.h>
int main()
{
char c;
printf("enter a character");
scanf("%ch",&c);
if((c>='a'&&c<='z')||(c>='A'&&C<='Z'))
{
printf("%ch is an alphabet",c);
}
else
{
printf(%ch is not an alphabet");
}
return 0;
}
